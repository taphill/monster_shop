<%= render partial: @render, locals: {
                                            path: profile_path,
                                            name_value: current_user.name,
                                            address_value: current_user.street_address,
                                            city_value: current_user.city,
                                            state_value: current_user.state,
                                            zip_value: current_user.zip,
                                            email_value: current_user.email,
                                            original_email_value: current_user.email
                                          } %>

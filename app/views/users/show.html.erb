<h1>Hello, <%= current_user.name %></h1>

<section class="user-profile">
  <h3>User Profile</h3>
  <%= link_to "Edit Password", profile_edit_path %>
  <%= link_to "Edit Profile Data", "/profile/edit?request=profile" %>
  <%= render 'shared/profile_data' %>
</section>
